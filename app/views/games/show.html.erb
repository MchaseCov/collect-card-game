<script id="game-data">
  const temporaryImagePath = "<%= asset_path('cardplaceholder.webp') %>";
  const player = <%= @first_person_player.id %>
  const game = <%= @game.id %>
</script>
<div id="game-container" class="absolute w-full h-full my-auto overflow-hidden bg-white bg-light-brown overscroll-y-none" data-controller="animations">
</div>
